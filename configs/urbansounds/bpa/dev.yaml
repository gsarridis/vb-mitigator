EXPERIMENT:
  NAME: "bpa"
  TAG: "dev"
  PROJECT: "urbansounds_baselines"
DATASET:
  TYPE: "urbansounds"
  BIASES: ["bias"]
  NUM_WORKERS: 0
MITIGATOR:
  TYPE: "bpa"
  BPA:
    CKPT_PATH: "./output/urbansounds_baselines/dev/erm/best"
    PER_CLUSTERS: 4
    UPDATE_CLUSTER_ITER: 10
    MOMENTUM: 0.3
    EXP_STEP: 0.05
SOLVER:
  BATCH_SIZE: 64
  EPOCHS: 100
  LR: 0.001
  TYPE: "Adam"
  WEIGHT_DECAY: 1e-4
  SCHEDULER:
    LR_DECAY_STAGES: [33, 66]
    LR_DECAY_RATE: 0.1
MODEL:
  TYPE: "cnn_classifier_mfcc"
METRIC: "wg_ovr"
LOG:
  SAVE_CRITERION: "test"
